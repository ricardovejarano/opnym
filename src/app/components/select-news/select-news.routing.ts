import { Routes } from '@angular/router';
import { SelectNewsComponent } from './select-news.component';

export const SelectNewsRoute: Routes = [
    {path: 'select', component: SelectNewsComponent, }
];
